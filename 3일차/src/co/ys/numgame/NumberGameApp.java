package co.ys.numgame;

public class NumberGameApp {
	
	// 임의의 수
	int com;  //난수
	int user; // 사용자 수 입력
	
	void init() {
		//컴퓨터가 만든 수(1~10)
	}
	
	void input() {
		//스캐너 정수
	}
	
	// 높다/낮다/정답 출력
	boolean confirm() {
		boolean result = ;
		if (com > user) {
			;
		} else if (com < user) {
			;
		} else {
			;
		}
		return 
	}
	void start() {
		init();
		while(true) {
			input();
			if(confirm()) {
				break;
			}
		}
	}
}
